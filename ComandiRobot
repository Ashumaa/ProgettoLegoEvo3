//Classe per la gestione del robot
public class RobotController {
    //Metodo per la connessione al robot
    public void connect() {
        //Codice per la connessione al robot tramite Bluetooth o WiFi
    }
    
    //Metodo per l'invio di comandi al robot
    public void sendCommand(String command) {
        //Codice per l'invio del comando al robot
    }
    
    //Metodo per la ricezione di dati dal robot
    public String receiveData() {
        //Codice per la ricezione dei dati dal robot
        return receivedData;
    }
    
    //Metodo per la disconnessione dal robot
    public void disconnect() {
        //Codice per la disconnessione dal robot
    }
}
